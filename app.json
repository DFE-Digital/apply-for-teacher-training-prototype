{
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "description": "Prototype for the ‘Apply for postgraduate teacher training’ service",
  "env": {
    "PASSWORD": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "Apply (Prototype)",
  "scripts": {
    "postdeploy": "node ./node_modules/gulp/bin/gulp generate-assets && node listen-on-port.js"
  },
  "stack": "heroku-18"
}
