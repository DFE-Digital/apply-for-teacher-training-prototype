{% extends "_content.njk" %}
{% set title = "Choose a course first" %}

{% block beforePageTitle %}
  {% if showCopiedBanner %}
    {{ appBanner({
      html: "<h2 class=\"govuk-heading-m\">Your new application is ready for editing</h2>",
      type: "success",
      icon: false
    }) }}
  {% else %}
    {{ appBanner({
      html: "<h2 class=\"govuk-heading-m\">Your " + serviceName + " account has been created</h2>",
      type: "success",
      icon: false
    }) }}
  {% endif %}
{% endblock %}

{% block primary %}
  <p>{{ serviceName }} is a new service that will eventually replace UCAS Teacher Training.</p>
  <p class="govuk-body">For now, you can only apply to some courses using {{ serviceName }}.</p>
  <p class="govuk-body">Choose a course first to check it’s available on {{ serviceName }} before you do your application.</p>
  <p class="govuk-body">If your course is not available, we’ll send you to UCAS to apply there.</p>

  <hr class="govuk-section-break govuk-section-break--m">
  {{ govukButton({
    classes: "govuk-!-margin-bottom-5",
    text: "Choose a course",
    href: "/application/" + applicationId + "/choices/add"
  }) }}
  <p class="govuk-body">or</p>
  <p class="govuk-body"><a href="/application/{{ applicationId }}">Go to your application form</a></p>
{% endblock %}
