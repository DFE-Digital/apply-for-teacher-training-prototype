{% extends "_form-wide.njk" %}

{% set applicationPath = "/application/" + applicationId %}
{% set formaction = applicationPath %}
{% set referrer = applicationPath + "/contact-information/review" %}
{% set title = "Contact information" %}
{% set canAmend = true %}

{% block pageNavigation %}
  {{ govukBackLink({
    href: "/application/" + applicationId,
    text: "Back to application"
  }) }}
{% endblock %}

{% block primary %}
  {% set contactDetails = applicationValue(["contactInformation"]) %}
  {% include "_includes/review/contact-information.njk" %}

  {{ govukCheckboxes({
    items: [{
      value: "true",
      text: "I have completed this section"
    }]
  } | decorateApplicationAttributes(["completed", "contactInformation"])) }}

  {{ govukButton({
    text: "Continue"
  }) }}
{% endblock %}
