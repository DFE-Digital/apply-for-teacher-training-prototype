{% extends "_form.njk" %}

{% set title = "Are you sure you want to delete this " + type + "?" %}

{% block pageNavigation %}
  {% if phase %}
    {{ govukBackLink({
      href: referrer + "?phase=" + phase
    }) }}
  {% else %}
    {{ govukBackLink({
      href: referrer
    }) }}
  {% endif %}
{% endblock %}

{% block primary %}
  {{ govukButton({
    text: "Yes I’m sure - delete this " + type,
    classes: "govuk-button--warning"
  }) }}
  <p class="govuk-body"><a href="{{ referrer }}">Cancel</a></p>
{% endblock %}
