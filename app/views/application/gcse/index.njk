{% extends "_form.njk" %}

{% set title = (id | capitalize) + " GCSE or equivalent" %}

{% block pageNavigation %}
  {% if referrer %}
    {{ govukBackLink({
      href: referrer
    }) }}
  {% else %}
    {{ govukBackLink({
      href: "/application/" + applicationId,
      text: "Back to application"
    }) }}
  {% endif %}
{% endblock %}

{% block primary %}
  {% set otherUkConditionalHtml %}
    {{ govukInput({
      label: {
        text: "Please specify type of qualification"
      }
    } | decorateApplicationAttributes(["gcse", id, "type-uk"])) }}
  {% endset %}
  {% set nonUkConditionalHtml %}
    {{ govukInput({
      label: {
        text: "Please specify type of qualification"
      }
    } | decorateApplicationAttributes(["gcse", id, "type-non-uk"])) }}
    {{ appAutocomplete({
      label: {
        text: "In which country did you gain this qualification?"
      },
      items: countries
    } | decorateApplicationAttributes(["gcse", id, "country"])) }}
    {{ govukInput({
      label: {
        text: "Awarding organisation?"
      }
    } | decorateApplicationAttributes(["gcse", id, "award-org"])) }}
  {% endset %}
  {% set missingConditionalHtml %}
    {{ govukCharacterCount({
      label: {
        html: "Explain how you plan to meet this eligibility requirement"
      },
      hint: {
        html: "For advice on missing qualifications, contact your chosen training provider or <a href=\"https://getintoteaching.education.gov.uk/get-help-and-support/\">Get into Teaching</a>."
      },
      maxwords: 200,
      rows: 12
    } | decorateApplicationAttributes(["gcse", id, "missing"])) }}
  {% endset -%}

  {{ govukRadios({
    fieldset: {
      legend: {
        text: "Type of qualification",
        classes: "govuk-label--m"
      }
    },
    items: [{
      text: "GCSE"
    }, {
      text: "GCE O Level"
    }, {
      text: "Scottish Higher"
    }, {
      text: "Other UK qualification",
      conditional: {
        html: otherUkConditionalHtml
      }
    }, {
      text: "Non-UK qualification",
      conditional: {
        html: nonUkConditionalHtml
      }
    }, {
      divider: "or"
    }, {
      text: "I don’t have an equivalent qualification yet",
      conditional: {
        html: missingConditionalHtml
      }
    }]
  } | decorateApplicationAttributes(["gcse", id, "type"])) }}

  {{ govukButton({
    text: "Save and continue"
  }) }}
{% endblock %}

{% block pageScripts %}
  {% set defaultValue = data.applications[applicationId]["gcse"][id]["country"] %}
  <script src="/public/javascripts/autocomplete.min.js"></script>
  <script>
    accessibleAutocomplete.enhanceSelectElement({
      defaultValue: '{{ data.applications[applicationid]["gcse"][id]["country"] }}',
      selectElement: document.querySelector('#gcse-{{ id }}-country')
    })
  </script>
{% endblock %}
