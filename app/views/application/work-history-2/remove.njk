{% extends "_form.njk" %}
{% set applicationPath = "/application/" + applicationId %}


{% set type = "entry" if item["category"] == "break" else "job" %}
{% set title = "Are you sure you want to delete this " + type + "?" %}

{% block pageNavigation %}
  {{ govukBackLink({
    href: applicationPath + "/work-history-2/review"
  }) }}
{% endblock %}

{% block primary %}
  {{ govukButton({
    text: "Yes I’m sure - delete this " + type,
    classes: "govuk-button--warning"
  }) }}
  <p class="govuk-body"><a href="{{ applicationPath + "/work-history-2/review" }}">Cancel</a></p>
{% endblock %}
